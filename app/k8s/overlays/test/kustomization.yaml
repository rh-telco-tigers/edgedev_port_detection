resources:
  - ../../base

patches:
  - path: deployment-patch.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: image-predictor

secretGenerator:
  - name: image-predictor-env
    behavior: create
    envs:
      - secrets.env
